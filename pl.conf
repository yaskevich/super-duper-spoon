{
  # Just a value
  db => "/d/plru/pl.db",
  log => "/var/log/mojo-pol.log",
  port => 4003,
  site => q{http://pol.dev/},
  mode => 'development', # 'production'
  

  # Nested data structures are fine too
  # baz => ['¦'],

  # You have full access to the application
  # music_dir => app->home->rel_dir('music')
};
